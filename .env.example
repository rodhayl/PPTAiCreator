# Example environment configuration for pptx-agent-langgraph
# Copy this file to .env and adjust values as needed.

# Offline by default.  Set to `online` to enable real web search and OpenRouter models.
MODE=offline

# Path to the SQLite database used for run checkpointing and logs
DB_PATH=checkpoints.db

# Base URL for Ollama models (offline).  See https://ollama.com for details.
OLLAMA_BASE=http://localhost:11434/v1

# Base URL and API key for OpenRouter (online).  Obtain your API key from https://openrouter.ai
OPENROUTER_BASE=https://openrouter.ai/api/v1
OPENROUTER_API_KEY=
